<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cpsc481.fall2016.uofcreccentret04">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".fitness_centre" />
        <activity android:name=".bookings" />
        <activity
            android:name=".activity_fitness_centre_t"
            android:label="@string/title_activity_activity_fitness_centre_t"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
